#include <stdio.h>

/**
 * main - entry
 * Return: 0
 */
int main(void)
{
	int p = 0;

	for (p = 0; p < 10; ++p)
	{
		putchar('0' + p);

		if (p != 9)
		{
			putchar(',');
			putchar(' ');
		}
	}

	putchar('\n');

	return (0);
}
